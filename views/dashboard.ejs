<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - JS Core</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        
        .navbar {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .nav-brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
            text-decoration: none;
        }
        
        .nav-links {
            display: flex;
            gap: 1rem;
            align-items: center;
        }
        
        .nav-link {
            color: white;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            transition: background 0.3s;
        }
        
        .nav-link:hover {
            background: rgba(255, 255, 255, 0.2);
        }
        
        .user-info {
            color: white;
            font-weight: 500;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .dashboard-header {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            color: white;
            text-align: center;
        }
        
        .dashboard-header h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            font-weight: 300;
        }
        
        .dashboard-header p {
            font-size: 1.1rem;
            opacity: 0.9;
        }
        
        .dashboard-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }
        
        .card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 2rem;
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        
        .card h3 {
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }
        
        .card p {
            opacity: 0.9;
            line-height: 1.6;
        }
        
        .user-details {
            background: rgba(255, 255, 255, 0.05);
            padding: 1rem;
            border-radius: 10px;
            margin-top: 1rem;
        }
        
        .user-details h4 {
            margin-bottom: 0.5rem;
        }
        
        .user-details p {
            margin-bottom: 0.5rem;
        }
        
        .btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 25px;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 1rem;
        }
        
        .btn-outline {
            border: 2px solid white;
            background: transparent;
            color: white;
        }
        
        .btn-outline:hover {
            background: white;
            color: #667eea;
        }
        
        .alert {
            padding: 1rem;
            margin: 1rem 0;
            border-radius: 8px;
            font-weight: 500;
        }
        
        .alert-success {
            background: rgba(212, 237, 218, 0.9);
            border: 1px solid #c3e6cb;
            color: #155724;
        }
        
        .alert-danger {
            background: rgba(248, 215, 218, 0.9);
            border: 1px solid #f5c6cb;
            color: #721c24;
        }
        
        @media (max-width: 768px) {
            .nav-container {
                padding: 0 1rem;
                flex-direction: column;
                gap: 1rem;
            }
            
            .dashboard-header h1 {
                font-size: 2rem;
            }
            
            .dashboard-content {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">JS Core</a>
            <div class="nav-links">
                <span class="user-info">Welcome, <%= user.name %>!</span>
                <a href="/" class="nav-link">Home</a>
                <a href="/auth/logout" class="nav-link">Logout</a>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
            <div class="alert alert-success"><%= success_msg %></div>
        <% } %>
        
        <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
            <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>
        
        <div class="dashboard-header">
            <h1>Dashboard</h1>
            <p>Welcome to your protected dashboard area</p>
        </div>
        
        <div class="dashboard-content">
            <div class="card">
                <h3>🔐 Authentication Status</h3>
                <p>You are successfully authenticated using Passport.js local strategy.</p>
                <div class="user-details">
                    <h4>Your Information:</h4>
                    <p><strong>Name:</strong> <%= user.name %></p>
                    <p><strong>Email:</strong> <%= user.email %></p>
                    <p><strong>User ID:</strong> <%= user.id %></p>
                    <p><strong>Registered:</strong> <%= new Date(user.createdAt).toLocaleDateString() %></p>
                </div>
            </div>
            
            <div class="card">
                <h3>🚀 Features</h3>
                <p>This dashboard demonstrates a complete authentication system with:</p>
                <ul style="margin: 1rem 0; padding-left: 1.5rem;">
                    <li>User registration with validation</li>
                    <li>Secure password hashing</li>
                    <li>Session management</li>
                    <li>Protected routes</li>
                    <li>Flash messages</li>
                    <li>Responsive design</li>
                </ul>
            </div>
            
            <div class="card">
                <h3>🛡️ Security</h3>
                <p>Your password is securely hashed using bcrypt, and your session is managed by Express Session middleware.</p>
                <div style="margin-top: 1rem;">
                    <a href="/auth/logout" class="btn btn-outline">Logout Securely</a>
                </div>
            </div>
            
            <div class="card">
                <h3>📚 About This Project</h3>
                <p>This is a JavaScript project showcasing Passport.js authentication implementation with Express.js and EJS templates.</p>
                <p style="margin-top: 1rem;">Built with modern web technologies and best practices for user authentication and session management.</p>
            </div>
        </div>
    </div>
</body>
</html>
